# Configuration de l'API PORELO
# Copiez ce fichier en .env et remplissez les valeurs

# Clé secrète pour signer les tokens JWT (MINIMUM 32 caractères aléatoires)
# Générer avec: openssl rand -base64 32
# OU en PowerShell: -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | % {[char]$_})
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-change-in-production

# URL de connexion à la base de données PostgreSQL
# Format: postgresql://user:password@host:port/database?schema=public
DATABASE_URL=postgresql://user:password@localhost:5432/porelo?schema=public

# Port du serveur (défaut: 8080)
PORT=8080

# Environnement (development, staging, production)
# En production, Swagger sera automatiquement désactivé
ENVIRONMENT=development

# Origines CORS autorisées (séparées par des virgules, SANS espaces)
# En développement: laisser vide pour accepter toutes les origines (*)
# En production: spécifier vos domaines exacts
# Exemple: https://porelo.com,https://app.porelo.com,https://admin.porelo.com
CORS_ALLOWED_ORIGINS=

# ============================================
# NOTES IMPORTANTES
# ============================================
# 
# 1. NE COMMITEZ JAMAIS le fichier .env dans Git
# 2. Le JWT_SECRET doit être unique et secret pour chaque environnement
# 3. En production, utilisez un gestionnaire de secrets (AWS Secrets Manager, HashiCorp Vault, etc.)
# 4. Pour générer un JWT_SECRET sécurisé en PowerShell:
#    [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
# 
# ============================================

